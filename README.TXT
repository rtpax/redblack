Red Black

This is an implementation of a red black tree. It is templated to accept any
type that is copy or move constructable. It will also takes a comparison 
functor as a template argument, using std::less by default.

It is essentially a clone of std::set
















